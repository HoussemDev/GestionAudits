{% extends 'base.html.twig' %}

{% block body %}


    <div class="container mt-4">



    </div>



    <div class="container theme-showcase" role="main">

        <div class="jumbotron">
            <div class="text-right">
                {% if finding.id  %}
                    <a href="{{ path('finding_show', {id: finding.id, slug: finding.slug}) }}" class="btn btn-info btn-sm">VIEW</a>
                {% endif %}
            </div>
            <div class="row">
                <div class="col-sm-4 dropdown-menu-sm-left">
                    {% if finding.id  %}

                    {{ include('Admin/Audit/auditleftmenu.html.twig') }}
                    {% endif %}
                </div>

                <div class="col-sm-8">

            {{ include('Admin/Finding/_formfinding.html.twig', {form: form, button: 'Save'}) }}
        </div>
    </div>
</div>
</div>











{% endblock %}



