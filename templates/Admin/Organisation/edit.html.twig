{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">



</div>


<div class="container theme-showcase" role="main">

    <div class="jumbotron">
        <div class="text-right">
            {% if org.id  %}


            <a href="{{ path('org.show', {id: org.id, slug: org.slug}) }}" class="btn btn-info btn-sm">VIEW</a>
        {% endif %}
        </div>
        <div class="row">
            <div class="col-sm-4 dropdown-menu-sm-left">
                {% if org.id  %}

                {{ include('Admin/Organisation/orgleftmenu.html.twig') }}
                {% endif %}
            </div>

            <div class="col-sm-8">

    <div class="container mt-4">

        {{ include('Admin/Organisation/_formorg.html.twig', {form: form, button: 'Save'}) }}



    </div></div>
        </div>
    </div></div>

{% endblock %}



