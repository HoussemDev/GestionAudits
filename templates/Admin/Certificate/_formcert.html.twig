{{ form_start(form) }}
<div class="row">
    <div class="col-md-12">{{ form_row(form.title) }}</div>
    <div class="col-md-12">{{ form_row(form.scopestatment) }}</div>
    <div class="col-md-12">{{ form_row(form.scope) }}</div>
    <div class="col-md-12">{{ form_row(form.issuedate) }}</div>
    <div class="col-md-12">{{ form_row(form.validuntil) }}</div>
    <div class="col-md-12">{{ form_row(form.Certificatstatus) }}</div>
    <div class="col-md-12">{{ form_row(form.initialcertdate) }}</div>
</div>


{{ form_widget(form) }}
<button class="btn btn-primary"> {{ button|default('Save') }} </button>
{{ form_end(form) }}