{% extends 'base.html.twig' %}
{% block title 'Audit' %}
{% block body %}

    <div class="container mt-4">
        {#<h1>Audit List</h1>#}
        {#{% for message in app.flashes('success') %}#}
        {#<div class="alert alert-success">#}
        {#{{ message }}#}
        {#</div>#}
        {#{% endfor %}#}
        {#<div class="text-right">#}
        {#<a href="{{ path('admin.audit.new') }}" class="btn btn-primary">Create New Audit</a>#}

        {#</div>#}
        {#<div class="jumbotron">#}
            {#<div class="container">#}
                {#{{ form_start(form) }}#}
                {#<div class="form-row">#}
                    {#<div class="col">#}
                        {#{{ form_row(form.audittitle) }}#}
                        {#{{ form_row(form.auditstandard) }}#}
                    {#</div>#}
                    {#<div class="col">#}
                        {#<button class="btn btn-primary">#}
                            {#Search#}
                        {#</button>#}

                    {#</div>#}
                {#</div>#}
                {#{{ form_end(form) }}#}

            {#</div>#}


        {#</div>#}



        <div class="jumbotron">
            <div class="container">
                {{ form_start(form) }}
                <div class="form-row">
                    <div class="col">
                        {{ form_row(form.audittitle) }}
                        {{ form_row(form.auditstandard) }}
                    </div>
                    <div class="col">
                        <button class="btn btn-primary">
                            Search
                        </button>
                        <input class="btn btn-danger" type="reset" value="Reset">


                    </div>
                </div>
                {{ form_end(form) }}

            </div>


        </div>
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Organisation</th>
                <th>Audit Title</th>
                <th>Standard</th>
                <th>Audit Status</th>
                <th>Audit Date</th>
            </tr>
            </thead>
            <tbody>
            {% for organisation in orgs %}
                {% for audit in organisation.audits %}
                    <tr>
                        <td>
                        <a href="{{ path('org.show', {id: organisation.id, slug: organisation.slug}) }}" >{{ organisation.name }} </a>
                        </td>
                        <td><a href="{{ path('audit.show', {id: audit.id, slug: audit.slug}) }}">{{ audit.Title }} </a>
                        </td>
                        <td>
                            {{ audit.standard }}
                        </td>
                        <td>
                            {{ audit.Status }}
                        </td>
                        <td>
                            {{ audit.auditdate|date('Y-m-d') }}
                        </td>

                    </tr>
                {% endfor %}
            {% endfor %}
            </tbody>

        </table>
        <div class="pagination">
            {{ knp_pagination_render(orgs) }}

        </div>

    </div>

{% endblock %}