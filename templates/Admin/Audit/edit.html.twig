{% extends 'base.html.twig' %}

{% block body %}


    <div class="container mt-4">



    </div>



    <div class="container theme-showcase" role="main">

        <div class="jumbotron">
            <div class="text-right">
                {% if audit.id  %}
                    <a href="{{ path('audit.show', {id: audit.id, slug: audit.slug}) }}" class="btn btn-info btn-sm">VIEW</a>
                {% endif %}
            </div>
            <div class="row">
                {% if audit.id  %}
                <div class="col-sm-4 dropdown-menu-sm-left">
                    {{ include('Admin/Audit/auditleftmenu.html.twig') }}
                </div>
                {% endif %}

                <div class="col-sm-8">

            {{ include('Admin/Audit/_formaudit.html.twig', {form: form, button: 'Save'}) }}
        </div>
    </div>
</div>
</div>











{% endblock %}



